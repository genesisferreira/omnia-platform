{
  "name": "@omnia/neurofrigo-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Neurofrigo Runtime MVP — orquestra Retrieval + Prompt + LLM (sem Payload/pgvector)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/test-*.ts",
    "bench": "tsx src/bench/basic-bench.ts",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@omnia/retrieval": "workspace:*"
  },
  "devDependencies": {
    "@omnia/typescript-config": "workspace:*",
    "@types/node": "^22.10.2",
    "rimraf": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
