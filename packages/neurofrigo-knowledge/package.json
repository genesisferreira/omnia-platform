{
  "name": "@omnia/neurofrigo-knowledge",
  "version": "0.0.0",
  "private": true,
  "description": "Neurofrigo Knowledge Hub — domain, workflow, ACL and future RAG ports (controlled)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/test-*.ts",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@omnia/typescript-config": "workspace:*",
    "@types/node": "^22.10.2",
    "eslint": "catalog:",
    "rimraf": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
