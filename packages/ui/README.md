# @omnia/ui

Design system da Omnia Platform baseado em shadcn/ui e Tailwind CSS.

## Estrutura

```
ui/
└── src/
    ├── components/    # Componentes React reutilizáveis
    ├── tokens/        # Design tokens (cores, tipografia, espaçamento)
    ├── hooks/         # React hooks compartilhados
    ├── icons/         # Ícones customizados
    ├── utils/         # Utilitários de UI (cn, formatters)
    └── index.ts       # Exports públicos
```

## Status

**Sprint 0** — Workspace configurado (`@omnia/ui`). Implementação na **Sprint 2**.
