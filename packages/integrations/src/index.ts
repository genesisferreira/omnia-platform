/**
 * @omnia/integrations — Conectores para serviços externos.
 *
 * Conectores planejados:
 * - deepseek/  — DeepSeek API (provider primário de IA)
 * - openai/    — OpenAI API (fallback)
 * - n8n/       — Webhooks e triggers n8n
 * - redis/     — Cache e sessões
 * - minio/     — Object storage (S3-compatible)
 * - email/     — SMTP / Resend / SendGrid
 * - whatsapp/  — WhatsApp Business API
 * - payload/   — Payload CMS adapter
 *
 * Implementação: Sprint 1+ (por conector).
 */
export {};
