/**
 * @omnia/logger — Logging centralizado estruturado.
 *
 * Estrutura planejada:
 * - transports/  — Console, arquivo, OpenTelemetry, Sentry
 * - formatters/  — JSON estruturado, redação de PII (LGPD)
 *
 * Padrão: logs JSON com correlationId, tenantId, userId, module.
 * Implementação: Sprint 1+.
 */
export {};
