# Omnia LMS Redis — cache + sessions (rede Docker interna only)
bind 0.0.0.0
protected-mode no
port 6379
tcp-backlog 511
timeout 0
tcp-keepalive 300

maxmemory 256mb
maxmemory-policy allkeys-lru

save ""
appendonly no
